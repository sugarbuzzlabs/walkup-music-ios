---
phase: 02-audio-engine
plan: 02
type: execute
---

<objective>
Build document picker for MP3 import from iOS Files app.

Purpose: Users need to import MP3 files from the Files app to assign walk-up songs to players.
Output: Reusable DocumentPicker SwiftUI view and integration with StorageManager's importSongFile.
</objective>

<context>
@.planning/phases/02-audio-engine/02-01-SUMMARY.md
@WalkoutDJ/Services/StorageManager.swift
@WalkoutDJ/Services/AudioManager.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SwiftUI document picker</name>
  <files>WalkoutDJ/Components/DocumentPicker.swift</files>
  <action>
    Create a DocumentPicker using UIViewControllerRepresentable wrapping UIDocumentPickerViewController:

    - Configure for .open mode with UTType.audio content types
    - Allow only single selection (allowsMultipleSelection = false)
    - On completion, return the selected URL via a callback closure
    - Handle cancellation gracefully (no-op)
    - The caller is responsible for security-scoped access (StorageManager.importSongFile already handles this)

    SwiftUI interface:
    DocumentPicker(onPicked: (URL) -> Void)
    Present via .sheet modifier.
  </action>
  <verify>Project builds with no errors</verify>
  <done>DocumentPicker compiles, can be presented as a sheet, returns selected file URL</done>
</task>

<task type="auto">
  <name>Task 2: Build and test on simulator</name>
  <files></files>
  <action>
    Build the project and install on simulator to verify everything compiles correctly with the new audio engine and document picker components.
    Run: xcodegen generate, xcodebuild for simulator, install and launch.
  </action>
  <verify>App launches on simulator with no crashes</verify>
  <done>App runs on simulator, all tabs still work, no runtime errors in console</done>
</task>

</tasks>

<verification>
- [ ] xcodebuild build succeeds
- [ ] DocumentPicker compiles with correct UIDocumentPickerViewController wrapping
- [ ] App launches on simulator without crashes
- [ ] Phase 2 complete
</verification>

<success_criteria>
- Document picker ready for use in Roster screen
- Audio engine ready for use in Game Day screen
- Clean build, no warnings
- Phase 2 complete — ready for Phase 3 (Roster)
</success_criteria>
